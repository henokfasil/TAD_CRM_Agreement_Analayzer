# Architecture

Phase 1 separates API routes, service logic, schemas, and persistence models. Streamlit pages are
thin UI entry points and should call backend services or API clients as the app matures.

