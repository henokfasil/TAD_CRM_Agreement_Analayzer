# Coding Methodology

The active research ontology lives in YAML under `config/codebooks/`. Application logic must load
variables from the codebook rather than hard-coding legal or thematic coding definitions.

